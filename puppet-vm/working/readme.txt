1. How to run a standalone puppet manifest file?

   $ puppet apply hello.pp

2. How to run a standalone puppet manifest file that uses hiera file?

   $ puppet apply hello.pp --environment=demo

3.  
